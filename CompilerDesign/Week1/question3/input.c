#include<stdio.h>
#define MAX 500 
int main() {
    char prod ; 
        int a ; 
    while( a > 0 ) {
       if ( a == 1 ) {
            printf(" %d what ", a); 
        }
        else (a == 2 ) {
            printf("%d yes ",a );
        }
    }
    exit(0) ; 
} 